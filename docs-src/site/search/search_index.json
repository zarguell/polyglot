{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Polyglot","text":"<p>AI-native secure application boilerplate.</p> <p>Clone it. Configure SSO + Postgres. Run Docker Compose. Log in. Then drop in a PRD and let an AI agent build your business app inside a hardened, well-documented scaffold.</p> <p></p>"},{"location":"#what-you-get","title":"What You Get","text":"<ul> <li>FastAPI app with async SQLAlchemy, Alembic, Pydantic v2</li> <li>SSO \u2014 OIDC (4 providers) + SAML + dev login</li> <li>RBAC \u2014 roles, permissions, <code>require_permission()</code> dependency</li> <li>TOTP MFA \u2014 QR setup, challenge flow, backup codes</li> <li>Postgres \u2014 data, tasks (Procrastinate), audit logs, all in one DB</li> <li>Security \u2014 CSP, CSRF, HSTS, security headers, audit logging, RLS docs</li> <li>HTMX + Jinja2 default frontend, React + Vite alt</li> <li>11 template packs \u2014 copy-on-activate: SMTP, Stripe, Webhooks, LDAP, etc.</li> <li>Docker Compose \u2014 app, worker, Postgres, optional profiles</li> </ul>"},{"location":"#quick-start","title":"Quick Start","text":"<pre><code>cp .env.example .env\n# Set SECRET_KEY, configure SSO or enable AUTH_DEV_MODE=true\ndocker compose up --build -d\ndocker compose exec app alembic upgrade head\n</code></pre> <p>Open http://localhost:8000.</p>"},{"location":"#screenshots","title":"Screenshots","text":"Home Login Dashboard"},{"location":"#architecture","title":"Architecture","text":"<pre><code>FastAPI + Postgres \u2192 Task Queue (Procrastinate) \u2192 Component System\n  \u251c\u2500\u2500 OIDC / SAML / Dev Auth\n  \u251c\u2500\u2500 RBAC (roles + permissions)\n  \u251c\u2500\u2500 TOTP MFA (on login)\n  \u251c\u2500\u2500 Audit logging (structured + DB)\n  \u2514\u2500\u2500 11 copy-on-activate templates\n</code></pre>"},{"location":"#license","title":"License","text":"<p>MIT</p>"},{"location":"architecture/components/","title":"Component System","text":""},{"location":"architecture/components/#the-registry-contract","title":"The Registry Contract","text":"<p>Every activated component lives in <code>app/components/&lt;name&gt;/</code> and exports a <code>register()</code> function:</p> <pre><code>def register(*, app: FastAPI, settings) -&gt; None:\n    \"\"\"Wire this component's routers, tasks, and middleware.\"\"\"\n    from .api import router\n    from .tasks import register_tasks\n    app.include_router(router, prefix=\"/api/&lt;name&gt;\")\n    register_tasks(task_app)\n</code></pre>"},{"location":"architecture/components/#discovery","title":"Discovery","text":"<p>On startup, <code>app/main.py</code> calls <code>_load_components()</code>:</p> <ol> <li>Scans <code>app/components/</code> for subpackages with <code>__init__.py</code></li> <li>Checks <code>INSTALLED_COMPONENTS</code> allowlist (if set in <code>.env</code>)</li> <li>Imports each component and calls its <code>register()</code> function</li> <li>Logs success or failure</li> </ol>"},{"location":"architecture/components/#allowlist-mode","title":"Allowlist Mode","text":"<pre><code>INSTALLED_COMPONENTS=smtp,stripe,webhooks\n</code></pre> <p>When set, only these components are loaded. All discovered components load when unset.</p>"},{"location":"architecture/components/#the-appcomponents-directory","title":"The <code>app/components/</code> Directory","text":"<p>At bootstrap, <code>app/components/</code> contains only <code>__init__.py</code>. Nothing is auto-loaded until you activate a template. Activated components are copied here from <code>boilerplate/templates/&lt;name&gt;/</code>.</p>"},{"location":"architecture/overview/","title":"Architecture Overview","text":""},{"location":"architecture/overview/#thesis","title":"Thesis","text":"<p>PostgreSQL is the foundational infrastructure primitive. The stack evolves only when complexity genuinely requires it.</p>"},{"location":"architecture/overview/#complexity-ladder","title":"Complexity Ladder","text":"<ol> <li>FastAPI + Postgres \u2014 the starting point</li> <li>+ Procrastinate \u2014 background tasks and scheduled work</li> <li>+ Template components \u2014 optional capability packs</li> <li>+ FSM/workflow libraries \u2014 stateful orchestration</li> <li>+ More worker containers \u2014 horizontal scale</li> <li>\u2192 Django \u2014 only when admin/workflow/metadata complexity justifies it</li> </ol>"},{"location":"architecture/overview/#stack","title":"Stack","text":"Layer Choice Web framework FastAPI (Starlette) ORM SQLAlchemy 2.0 async + asyncpg Migrations Alembic Auth Authlib (OIDC + SAML) Sessions Starlette SessionMiddleware (signed cookie) Task queue Procrastinate (Postgres-backed) Frontend (default) HTMX 2 + Jinja2 + Tailwind v3 Frontend (alt) React 18 + Vite + TypeScript Logging structlog Container Docker Compose with profiles Database Postgres 16"},{"location":"architecture/overview/#module-layout","title":"Module Layout","text":"<pre><code>polyglot-stack/\n\u251c\u2500\u2500 app/\n\u2502   \u251c\u2500\u2500 api/           # Route handlers (public, auth, system, admin)\n\u2502   \u251c\u2500\u2500 core/          # Config, DB, auth, middleware, tasks, templates\n\u2502   \u251c\u2500\u2500 models/        # SQLAlchemy ORM models\n\u2502   \u251c\u2500\u2500 services/      # Business logic layer\n\u2502   \u251c\u2500\u2500 components/    # Activated template modules (auto-registered)\n\u2502   \u251c\u2500\u2500 templates/     # Jinja2 HTML templates\n\u2502   \u2514\u2500\u2500 static/        # Compiled CSS\n\u251c\u2500\u2500 alembic/           # DB migrations\n\u251c\u2500\u2500 boilerplate/       # Copy-on-activate template packs\n\u251c\u2500\u2500 frontend/          # React + Vite (separate profile)\n\u251c\u2500\u2500 tests/             # pytest suite\n\u2514\u2500\u2500 docs/              # Project documentation\n</code></pre>"},{"location":"architecture/overview/#component-system","title":"Component System","text":"<p><code>app/components/</code> is the integration point for activated templates:</p> <pre><code># app/components/__init__.py (simplified)\ndef load_components(app, settings):\n    for name in discover_components():\n        if settings.installed_components and name not in settings.installed_components:\n            continue\n        module = import_module(f\"app.components.{name}\")\n        module.register(app=app, settings=settings)\n</code></pre> <p>Each component self-registers its routers, tasks, and middleware via a <code>register()</code> function. No manual wiring needed.</p>"},{"location":"architecture/security/","title":"Security Model","text":""},{"location":"architecture/security/#middleware-chain-inbound-order","title":"Middleware Chain (Inbound Order)","text":"<ol> <li>RequestIdMiddleware \u2014 injects <code>X-Request-ID</code>, sets structlog context</li> <li>StructuredLoggingMiddleware \u2014 logs method, path, status, latency</li> <li>NonceMiddleware \u2014 per-request CSP nonce</li> <li>CSRFMiddleware \u2014 double-submit cookie pattern</li> <li>SessionMiddleware \u2014 signed Starlette session cookie</li> <li>SecurityHeadersMiddleware \u2014 CSP, HSTS, XFO, etc.</li> </ol>"},{"location":"architecture/security/#content-security-policy","title":"Content Security Policy","text":"<pre><code>default-src 'self'\nstyle-src 'self'\nscript-src 'self' 'nonce-{random}'\nframe-ancestors 'none'\nbase-uri 'self'\n</code></pre> <p>The only inline script is the CSRF bootstrap \u2014 protected by a per-request nonce.</p>"},{"location":"architecture/security/#csrf-protection","title":"CSRF Protection","text":"<ul> <li>Token stored in server session</li> <li>Every form includes hidden <code>csrf_token</code> input</li> <li>HTMX requests use auto-injected <code>X-CSRFToken</code> header</li> <li>Exemptions: <code>/auth/callback</code> (OIDC redirect), <code>/auth/saml/acs</code> (IdP POST), <code>/webhooks/*</code> (HMAC)</li> </ul>"},{"location":"architecture/security/#session-security","title":"Session Security","text":"<ul> <li>HttpOnly \u2014 not accessible to JavaScript</li> <li>Secure \u2014 in non-local environments</li> <li>SameSite=Lax \u2014 CSRF protection for top-level navigations</li> <li>12-hour max age \u2014 configurable via <code>SESSION_MAX_AGE_SECONDS</code></li> <li>Server-side revocation \u2014 sessions can be invalidated individually</li> </ul>"},{"location":"architecture/security/#audit-logging","title":"Audit Logging","text":"<p>Every auth-relevant event is logged to both:</p> <ul> <li>Structured logs (JSON in production, console in local)</li> <li><code>audit_logs</code> table in Postgres with actor, action, target, IP, request ID</li> </ul> <p>Events tracked: login, logout, failed login, settings change, component activation.</p>"},{"location":"architecture/security/#role-based-access-control","title":"Role-Based Access Control","text":"<pre><code># Usage in routes\nfrom app.api.deps import require_permission\n\nDeleteUsers = Annotated[User, Depends(require_permission(\"users\", \"delete\"))]\n\n@router.delete(\"/users/{id}\")\nasync def delete_user(user: DeleteUsers, id: UUID):\n    ...\n</code></pre> <p>Built-in roles: <code>admin</code> (full access), <code>user</code> (base role). Roles and permissions are stored in Postgres tables with many-to-many relationships.</p>"},{"location":"architecture/security/#row-level-security-rls","title":"Row-Level Security (RLS)","text":"<p>For multi-tenant deployments, Postgres RLS is documented as an additional defense layer:</p> <ul> <li>Service-layer tenant scoping is the primary mechanism</li> <li>RLS is defense-in-depth, not a replacement</li> <li>Policies are implemented via Alembic migrations with <code>op.execute()</code></li> <li>Session variable <code>app.tenant_id</code> is set on each authenticated request</li> </ul>"},{"location":"auth/rbac/","title":"RBAC \u2014 Role-Based Access Control","text":""},{"location":"auth/rbac/#models","title":"Models","text":"<pre><code>Role (id, name, description, is_system, created_at)\nPermission (id, resource, action, description, created_at)\nUserRole (user_id, role_id)          # join table\nRolePermission (role_id, permission_id)  # join table\n</code></pre>"},{"location":"auth/rbac/#service-functions","title":"Service Functions","text":"<pre><code>from app.services.rbac_service import (\n    grant_role, revoke_role,\n    has_permission, get_user_permissions,\n    create_role, create_permission,\n)\n</code></pre>"},{"location":"auth/rbac/#dependency","title":"Dependency","text":"<pre><code>from app.api.deps import require_permission\n\n# Create a dependency that checks a specific permission\nAdminDeleteUsers = Annotated[User, Depends(require_permission(\"users\", \"delete\"))]\n\n@router.delete(\"/users/{id}\")\nasync def delete_user(user: AdminDeleteUsers, id: UUID):\n    ...\n</code></pre> <p>The <code>require_permission</code> factory returns a FastAPI dependency that:</p> <ol> <li>Resolves the current user</li> <li>Checks <code>is_admin</code> \u2014 admins bypass all permission checks</li> <li>Queries the RBAC join tables for the specific permission</li> <li>Returns 403 if the user lacks the permission</li> </ol>"},{"location":"auth/rbac/#admin-routes","title":"Admin Routes","text":"<p>When the RBAC system is active, admin CRUD routes are available under <code>/api/admin/</code>:</p> <ul> <li><code>GET /api/admin/roles</code> \u2014 list roles with permissions</li> <li><code>POST /api/admin/roles</code> \u2014 create role</li> <li><code>POST /api/admin/roles/{id}/permissions</code> \u2014 assign permissions</li> <li><code>GET /api/admin/permissions</code> \u2014 list permissions</li> <li><code>POST /api/admin/permissions</code> \u2014 create permission</li> <li><code>POST /api/admin/users/{id}/roles</code> \u2014 grant role to user</li> <li><code>DELETE /api/admin/users/{id}/roles/{role_id}</code> \u2014 revoke role</li> </ul>"},{"location":"getting-started/auth-setup/","title":"Authentication Setup","text":"<p>Polyglot supports three authentication modes. Choose one:</p>"},{"location":"getting-started/auth-setup/#1-dev-login-local-only","title":"1. Dev Login (Local Only)","text":"<p>For development without an identity provider:</p> <pre><code>AUTH_DEV_MODE=true\nENVIRONMENT=local\n</code></pre> <p>Visit <code>/login</code> \u2014 a simple form creates a session for any email. The first user becomes admin.</p> <p>Never enable AUTH_DEV_MODE in production</p> <p>The app asserts <code>ENVIRONMENT != production</code> when dev mode is on and refuses to boot.</p>"},{"location":"getting-started/auth-setup/#2-oidc-sso","title":"2. OIDC SSO","text":"<p>Polyglot ships with 4 OIDC provider presets:</p> Provider <code>AUTH_OIDC_PROVIDER</code> Required Env Generic <code>generic</code> <code>AUTH_OIDC_DISCOVERY_URL</code> Microsoft Entra ID <code>entra</code> <code>AUTH_OIDC_TENANT</code> Okta <code>okta</code> <code>AUTH_OIDC_DOMAIN</code> Google Workspace <code>google</code> \u2014 (uses Google's discovery URL) <p>All providers require: <pre><code>AUTH_OIDC_CLIENT_ID=your-client-id\nAUTH_OIDC_CLIENT_SECRET=your-client-secret\n</code></pre></p> <p>The login flow:</p> <ol> <li>User clicks Sign In \u2192 redirected to IdP</li> <li>IdP authenticates \u2192 redirects to <code>/auth/callback</code></li> <li>App exchanges code for tokens, upserts user, creates session</li> <li>User lands on dashboard</li> </ol>"},{"location":"getting-started/auth-setup/#3-saml-sso","title":"3. SAML SSO","text":"<p>Enable SAML when you need to integrate with a SAML 2.0 identity provider:</p> <pre><code>AUTH_SAML_ENABLED=true\nAUTH_SAML_IDP_METADATA_URL=https://your-idp.com/metadata\nAUTH_SAML_SP_ENTITY_ID=polyglot\nAUTH_SAML_SP_PRIVATE_KEY=...  # PEM-encoded\nAUTH_SAML_SP_CERT=...         # PEM-encoded\n</code></pre> <p>The SAML SP metadata endpoint is at <code>/auth/saml/metadata</code> \u2014 register this with your IdP.</p>"},{"location":"getting-started/auth-setup/#auto-provisioning","title":"Auto-Provisioning","text":"<p>The first user to authenticate (via any method) is automatically granted <code>is_admin=true</code>. Subsequent users get <code>is_admin=false</code> by default.</p>"},{"location":"getting-started/configuration/","title":"Configuration","text":"<p>Polyglot uses <code>pydantic-settings</code> to read configuration from environment variables or <code>.env</code> file.</p>"},{"location":"getting-started/configuration/#core-settings","title":"Core Settings","text":"Variable Type Default Description <code>APP_NAME</code> str <code>Polyglot</code> Application display name <code>ENVIRONMENT</code> enum <code>local</code> <code>local</code>, <code>dev</code>, <code>staging</code>, <code>production</code> <code>SECRET_KEY</code> SecretStr \u2014 Required. Min 32 chars in production <code>ENABLE_OPENAPI</code> bool <code>True</code> local only Enable <code>/docs</code> and <code>/redoc</code> <code>DATABASE_URL</code> PostgresDsn \u2014 Async Postgres connection string"},{"location":"getting-started/configuration/#auth-settings","title":"Auth Settings","text":"Variable Type Default Description <code>AUTH_DEV_MODE</code> bool <code>False</code> Dev login bypass (local only) <code>AUTH_OIDC_PROVIDER</code> enum <code>generic</code> <code>generic</code>, <code>entra</code>, <code>okta</code>, <code>google</code> <code>AUTH_OIDC_CLIENT_ID</code> str \u2014 OIDC client ID <code>AUTH_OIDC_CLIENT_SECRET</code> SecretStr \u2014 OIDC client secret <code>AUTH_DEV_MODE</code> bool <code>False</code> Dev login bypass (local only) <code>AUTH_SAML_ENABLED</code> bool <code>False</code> Enable SAML SSO <code>AUTH_SAML_IDP_METADATA_URL</code> str \u2014 IdP metadata URL <code>SESSION_MAX_AGE_SECONDS</code> int <code>43200</code> 12 hours"},{"location":"getting-started/configuration/#security-settings","title":"Security Settings","text":"Variable Type Default Description <code>ALLOWED_HOSTS</code> list <code>localhost,127.0.0.1</code> Trusted host validation <code>CORS_ALLOWED_ORIGINS</code> list \u2014 CORS origins (SPA mode)"},{"location":"getting-started/configuration/#component-settings","title":"Component Settings","text":"Variable Type Default Description <code>INSTALLED_COMPONENTS</code> list \u2014 Explicit allowlist (comma-separated)"},{"location":"getting-started/configuration/#startup-safety-checks","title":"Startup Safety Checks","text":"<p>The app refuses to boot if:</p> <ul> <li><code>ENVIRONMENT=production</code> and <code>AUTH_DEV_MODE=true</code> \u2014 dev mode forbidden in prod</li> <li><code>ENVIRONMENT != local</code> and <code>SECRET_KEY &lt; 32 chars</code> \u2014 weak secret</li> <li><code>ENVIRONMENT != local</code> \u2014 automatically disables OpenAPI docs in non-local</li> </ul>"},{"location":"getting-started/quickstart/","title":"Quick Start","text":""},{"location":"getting-started/quickstart/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.12+</li> <li>Docker + Docker Compose</li> <li><code>uv</code> (Python package manager)</li> </ul>"},{"location":"getting-started/quickstart/#clone-and-setup","title":"Clone and Setup","text":"<pre><code>git clone https://github.com/zarguell/polyglot.git\ncd polyglot\ncp .env.example .env\n</code></pre> <p>Edit <code>.env</code> and set at minimum:</p> <pre><code>SECRET_KEY=your-32-char-random-string-here\nENVIRONMENT=local\nAUTH_DEV_MODE=true      # enables dev login (no SSO config needed)\n</code></pre>"},{"location":"getting-started/quickstart/#start-the-stack","title":"Start the Stack","text":"<pre><code>docker compose up --build -d\n</code></pre> <p>This starts:</p> <ul> <li>Postgres 16 on port 5432</li> <li>App (FastAPI + Uvicorn) on port 8000</li> <li>Worker (Procrastinate) for background tasks</li> </ul>"},{"location":"getting-started/quickstart/#apply-migrations","title":"Apply Migrations","text":"<pre><code>docker compose exec app alembic upgrade head\n</code></pre>"},{"location":"getting-started/quickstart/#visit-the-app","title":"Visit the App","text":"Page URL Home http://localhost:8000 Dev Login http://localhost:8000/login Health http://localhost:8000/healthz API Docs http://localhost:8000/docs"},{"location":"getting-started/quickstart/#create-your-first-user","title":"Create Your First User","text":"<ol> <li>Navigate to http://localhost:8000/login</li> <li>Enter an email and display name</li> <li>Click Sign in (Dev)</li> <li>You'll land on the dashboard \u2014 you're now authenticated as admin</li> </ol>"},{"location":"getting-started/quickstart/#next-steps","title":"Next Steps","text":"<ul> <li>Read Configuration for environment variables</li> <li>Set up real SSO with OIDC or SAML</li> <li>Explore available templates you can activate</li> <li>Drop a <code>docs/PRD.md</code> into the repo and let an AI agent build your business logic</li> </ul>"},{"location":"getting-started/templates/","title":"Template Activation","text":"<p>Polyglot ships 11 optional template packs in <code>boilerplate/templates/</code>. Each adds a specific capability and is activated via copy-on-activate \u2014 files are copied into <code>app/components/</code> and the component auto-registers.</p>"},{"location":"getting-started/templates/#activation-command","title":"Activation Command","text":"<pre><code>make activate-component COMPONENT=&lt;name&gt;\n</code></pre> <p>Or manually:</p> <pre><code>bash scripts/activate_component.sh &lt;name&gt;\n</code></pre>"},{"location":"getting-started/templates/#available-templates","title":"Available Templates","text":"Template What it Adds External Deps <code>smtp</code> Email sending (aiosmtplib), templates, Mailhog profile SMTP server <code>file_storage</code> File upload/download, local/S3 backends S3 (optional) <code>redis_cache</code> Caching, rate limiter Redis <code>stripe</code> Checkout sessions, webhooks, subscription models Stripe API key <code>websockets</code> Real-time WebSocket connections None <code>fsm_workflows</code> Finite state machine engine (transitions lib) None <code>reporting_exports</code> CSV/XLSX/PDF report generation None <code>inbound_webhooks</code> HMAC-verified webhook receiver None <code>outbound_webhooks</code> Webhook dispatch with retry/backoff None <code>ldap_ad</code> LDAP user directory sync LDAP server <code>django_upgrade</code> Migration guide to Django (docs-only) None <code>totp_mfa</code> TOTP 2FA setup, challenge, backup codes None"},{"location":"getting-started/templates/#activation-steps","title":"Activation Steps","text":"<p>Each template's <code>ACTIVATE.md</code> documents exact steps, but the general process:</p> <ol> <li>Copy \u2014 <code>scripts/activate_component.sh</code> copies source into <code>app/components/&lt;name&gt;/</code></li> <li>Env \u2014 Add env vars from <code>env.additions</code> to <code>.env</code></li> <li>Compose \u2014 Merge <code>compose.fragment.yml</code> into <code>docker-compose.override.yml</code></li> <li>Migrate \u2014 Copy migration files and run <code>alembic upgrade head</code></li> <li>Register \u2014 The component's <code>register()</code> function auto-wires routes and tasks</li> </ol>"},{"location":"getting-started/templates/#how-it-works","title":"How It Works","text":"<p>Each component exposes a <code>register()</code> function:</p> <pre><code>def register(*, app: FastAPI, settings) -&gt; None:\n    \"\"\"Wire the component's routers, tasks, and middleware.\"\"\"\n    from .api import router\n    app.include_router(router, prefix=\"/api/&lt;name&gt;\")\n</code></pre> <p>The app's component registry in <code>app/components/__init__.py</code>:</p> <ol> <li>Discovers all subpackages in <code>app/components/</code></li> <li>Checks <code>INSTALLED_COMPONENTS</code> allowlist (if set)</li> <li>Calls each active component's <code>register()</code> on startup</li> </ol>"},{"location":"operations/docker/","title":"Docker Compose","text":""},{"location":"operations/docker/#services","title":"Services","text":"<pre><code>services:\n  postgres:   # Postgres 16 on port 5432\n  app:        # FastAPI on port 8000\n  worker:     # Procrastinate background worker\n  mailhog:    # SMTP test server (profile: mailhog)\n  pgadmin:    # DB admin UI (profile: pgadmin)\n</code></pre>"},{"location":"operations/docker/#commands","title":"Commands","text":"<pre><code>make up        # Start all services\nmake down      # Stop all services\nmake logs      # Tail logs\nmake build     # Rebuild images\n</code></pre>"},{"location":"operations/docker/#profiles","title":"Profiles","text":"<p>Optional services are behind Docker Compose profiles:</p> <pre><code># With SMTP testing\ndocker compose --profile mailhog up -d\n\n# With pgAdmin\ndocker compose --profile pgadmin up -d\n\n# With React frontend dev server\ndocker compose --profile react up -d\n</code></pre>"},{"location":"operations/docker/#volumes","title":"Volumes","text":"<ul> <li><code>pgdata</code> \u2014 Postgres data persisted across restarts</li> <li>Code is mounted as a bind volume in local mode for hot reloading</li> </ul>"}]}